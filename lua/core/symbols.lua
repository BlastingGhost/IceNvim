-- ==============================================
-- 图标配置表：统一管理所有插件和配置中使用的 Nerd Font 图标
-- 作用：所有插件（如 LSP 诊断、文件树、状态栏）都从这里获取图标，确保风格一致
-- 核心说明：
-- 1. 图标格式：均为 Nerd Font 标准图标（需安装 Nerd Font 字体才能正常显示）
-- 2. 命名规范：按功能/类型命名（如 Error 对应错误提示、File 对应文件图标），易维护
-- 3. 适用场景：LSP 诊断信息、Telescope 搜索结果、Nvim-Tree 文件树、代码语法高亮等
-- 新手注意：若图标显示乱码，需安装 Nerd Font 并在终端中设置使用；下方提供 FiraCode 兼容版替换方案
-- ==============================================

-- 原图标配置表（已注释，新手可直接替换为下方 FiraCode 兼容版）
-- Ice.symbols = {
--     Affirmative = "✓ ",          -- 确认/成功标记（如操作成功提示）
--     Array = " ",               -- 数组类型图标（代码语法高亮、LSP 类型提示）
--     Boolean = " ",              -- 布尔类型图标（true/false 相关提示）
--     Class = "󰠱 ",                -- 类类型图标（面向对象编程中类的标记）
--     CodeAction = " ",           -- 代码操作图标（LSP 代码修复建议）
--     Color = "󰏘 ",                -- 颜色图标（颜色选择器、CSS 颜色属性提示）
--     Component = "󰡀 ",            -- 组件图标（前端框架组件，如 Vue/React 组件）
--     Constant = "󰏿 ",             -- 常量图标（代码中常量语法高亮）
--     Constructor = " ",          -- 构造函数图标（类的构造方法标记）
--     Definition = " ",           -- 定义图标（跳转到定义功能的标记）
--     Diagnostic = " ",           -- 诊断图标（LSP 诊断信息总标记）
--     Dos = " ",                  -- Windows 系统图标（系统相关提示）
--     Enum = " ",                 -- 枚举类型图标（代码中 enum 类型标记）
--     EnumMember = " ",           -- 枚举成员图标（枚举中的属性标记）
--     Error = " ",                -- 错误图标（LSP 错误诊断、操作失败提示）
--     Event = " ",                -- 事件图标（代码中事件定义/触发标记）
--     Field = "󰜢 ",                -- 字段图标（类/对象的属性标记）
--     File = "󰈙 ",                 -- 文件图标（文件树、文件搜索结果）
--     Folder = "󰉋 ",               -- 文件夹图标（文件树中的目录标记）
--     Fragment = " ",             -- 代码片段图标（Snippet 插件相关）
--     Function = "󰊕 ",             -- 函数图标（代码中函数定义标记）
--     Hint = " ",                 -- 提示图标（LSP 辅助提示信息）
--     Info = " ",                 -- 信息图标（普通通知、说明类提示）
--     Interface = " ",            -- 接口图标（面向对象编程中接口标记）
--     Key = " ",                  -- 键/关键字图标（代码中关键字、字典的键标记）
--     Keyword = "󰌋 ",              -- 关键字图标（语法高亮中的关键字标记）
--     LspSagaFinder = " ",        -- LspSaga 搜索图标（LspSaga 插件的查找功能标记）
--     Mac = " ",                  -- MacOS 系统图标（系统相关提示）
--     Method = "󰆧 ",               -- 方法图标（类中的方法标记）
--     Module = "󰆧 ",               -- 模块图标（代码中模块/包的标记）
--     Namespace = " ",            -- 命名空间图标（代码中命名空间标记）
--     Negative = "✗ ",             -- 否定/失败标记（操作失败提示）
--     Null = "󰟢 ",                 -- 空值图标（null/undefined 类型标记）
--     Number = " ",               -- 数字类型图标（代码中数字类型标记）
--     Object = " ",               -- 对象图标（代码中对象类型标记）
--     Operator = "󰆕 ",             -- 运算符图标（语法高亮中的运算符标记）
--     Package = " ",              -- 包图标（依赖包、插件包标记）
--     Pending = "➜ ",              -- 等待/加载中标记（操作执行中提示）
--     Property = "󰜢 ",             -- 属性图标（对象/类的属性标记，同 Field）
--     Reference = "󰈇 ",            -- 引用图标（代码中变量/函数的引用标记）
--     RenamePrompt = "➤ ",         -- 重命名提示图标（LSP 重命名功能的输入提示）
--     Snippet = " ",              -- 代码片段图标（代码片段插入提示）
--     String = " ",               -- 字符串类型图标（代码中字符串类型标记）
--     Struct = "󰙅 ",               -- 结构体图标（代码中结构体类型标记）
--     Text = "󰉿 ",                 -- 文本图标（普通文本内容标记）
--     TypeParameter = "󰉺 ",        -- 类型参数图标（泛型编程中的类型参数标记）
--     Unit = "󰑭 ",                 -- 单位图标（代码中单位相关属性标记）
--     Unix = " ",                 -- Unix/Linux 系统图标（系统相关提示）
--     Value = "󰎠 ",                -- 值图标（普通值类型标记）
--     Variable = "󰀫 ",             -- 变量图标（代码中变量定义标记）
--     Warn = " ",                 -- 警告图标（LSP 警告诊断、潜在问题提示）
-- }

-- ==============================================
-- FiraCode 字体兼容版图标配置（已安装 FiraCode 仍乱码时使用）
-- 核心优化：
-- 1. 选用 FiraCode 原生支持的图标（无需额外 Nerd Font 补丁，直接渲染）
-- 2. 保持原功能命名和使用场景不变，替换后所有插件自动适配
-- 3. 图标风格统一：以简洁符号为主，避免复杂图标导致的渲染异常
-- 使用方法：取消下方注释，注释掉上面的原配置表，保存后重启 Neovim 即可
-- ==============================================
Ice.symbols = {
    Affirmative = "✓ ",          -- 确认/成功标记（FiraCode 原生支持，无乱码）
    Array = "[] ",               -- 数组类型图标（用方括号替代复杂图标，直观且兼容）
    Boolean = "◊ ",              -- 布尔类型图标（菱形符号，FiraCode 完美渲染）
    Class = "⦿ ",                -- 类类型图标（实心圆，代表类的封装特性）
    CodeAction = "⚡ ",           -- 代码操作图标（闪电符号，代表快速修复）
    Color = "🎨 ",               -- 颜色图标（调色盘符号，直观表示颜色相关）
    Component = "🗂️ ",            -- 组件图标（文件夹符号，代表组件分类）
    Constant = "🔒 ",             -- 常量图标（锁符号，代表常量不可修改）
    Constructor = "🏗️ ",          -- 构造函数图标（建筑符号，代表创建实例）
    Definition = "📍 ",           -- 定义图标（定位符号，代表跳转到定义）
    Diagnostic = "ℹ️ ",           -- 诊断图标（信息符号，代表诊断提示）
    Dos = "🪟 ",                  -- Windows 系统图标（窗口符号，代表 Windows）
    Enum = "🔢 ",                 -- 枚举类型图标（数字符号，代表枚举序列）
    EnumMember = "🔢 ",           -- 枚举成员图标（同 Enum，保持一致性）
    Error = "❌ ",                -- 错误图标（叉号，FiraCode 原生支持，清晰醒目）
    Event = "⚡ ",                -- 事件图标（闪电符号，代表事件触发）
    Field = "📌 ",                -- 字段图标（图钉符号，代表对象属性）
    File = "📄 ",                 -- 文件图标（文件符号，直观表示文件）
    Folder = "📁 ",               -- 文件夹图标（文件夹符号，FiraCode 完美渲染）
    Fragment = "🧩 ",             -- 代码片段图标（拼图符号，代表代码片段）
    Function = "🔧 ",             -- 函数图标（工具符号，代表功能实现）
    Hint = "💡 ",                 -- 提示图标（灯泡符号，代表辅助提示）
    Info = "ℹ️ ",                 -- 信息图标（信息符号，代表普通通知）
    Interface = "🔗 ",            -- 接口图标（链接符号，代表接口关联）
    Key = "🔑 ",                  -- 键/关键字图标（钥匙符号，代表关键字）
    Keyword = "🔤 ",              -- 关键字图标（字母符号，代表语法关键字）
    LspSagaFinder = "🔍 ",        -- LspSaga 搜索图标（放大镜符号，代表查找）
    Mac = "🍎 ",                  -- MacOS 系统图标（苹果符号，代表 Mac）
    Method = "⚙️ ",               -- 方法图标（齿轮符号，代表类的方法）
    Module = "📦 ",               -- 模块图标（包裹符号，代表模块/包）
    Namespace = "📂 ",            -- 命名空间图标（文件夹符号，代表命名空间）
    Negative = "✗ ",             -- 否定/失败标记（叉号，代表操作失败）
    Null = "∅ ",                  -- 空值图标（空集符号，代表 null/undefined）
    Number = "🔢 ",               -- 数字类型图标（数字符号，代表数字类型）
    Object = "🗃️ ",               -- 对象图标（文件柜符号，代表对象存储）
    Operator = "⚡ ",             -- 运算符图标（闪电符号，代表运算操作）
    Package = "📦 ",              -- 包图标（包裹符号，代表依赖包）
    Pending = "⏳ ",              -- 等待/加载中标记（沙漏符号，代表加载中）
    Property = "📌 ",             -- 属性图标（同 Field，保持一致性）
    Reference = "↩️ ",             -- 引用图标（返回符号，代表引用关联）
    RenamePrompt = "➤ ",         -- 重命名提示图标（箭头符号，代表输入提示）
    Snippet = "🧩 ",              -- 代码片段图标（同 Fragment，保持一致性）
    String = "📝 ",               -- 字符串类型图标（笔记符号，代表文本内容）
    Struct = "🏗️ ",               -- 结构体图标（建筑符号，代表结构体）
    Text = "📄 ",                 -- 文本图标（文件符号，代表普通文本）
    TypeParameter = "🔠 ",        -- 类型参数图标（字母符号，代表泛型参数）
    Unit = "📏 ",                 -- 单位图标（尺子符号，代表单位属性）
    Unix = "🐧 ",                 -- Unix/Linux 系统图标（企鹅符号，代表 Linux）
    Value = "💎 ",                -- 值图标（宝石符号，代表值类型）
    Variable = "🔄 ",             -- 变量图标（循环符号，代表变量可修改）
    Warn = "⚠️ ",                 -- 警告图标（警告符号，FiraCode 原生支持，醒目）
}