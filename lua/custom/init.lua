-- ==============================================
-- 图标配置表：统一管理所有插件和配置中使用的 Nerd Font 图标
-- 作用：所有插件（如 LSP 诊断、文件树、状态栏、代码高亮）均从这里获取图标，确保风格统一
-- 核心说明：
-- 1. 图标格式：基于 Nerd Font 标准图标（需安装带 Nerd Font 补丁的字体才能正常显示）
-- 2. 命名规范：按功能/类型命名（如 Error 对应错误提示、File 对应文件图标），便于维护和调用
-- 3. 适用场景：LSP 诊断信息、Telescope 搜索结果、Nvim-Tree 文件树、代码语法高亮、插件提示等
-- 新手注意：
-- - 若图标显示乱码，大概率是字体未安装或终端未配置 Nerd Font
-- - 下方提供「Linux FiraCode 兼容版」图标（无需 Nerd Font 补丁，原生渲染无乱码），直接替换即可使用
-- ==============================================

-- 原图标配置表（已注释，新手可直接替换为下方 FiraCode 兼容版）
-- Ice.symbols = {
--   Affirmative = "✓",          -- 确认/成功标记（如操作成功反馈、勾选状态）
--   Array = "",               -- 数组类型图标（代码语法高亮、LSP 类型提示、数据结构标记）
--   Boolean = "蘒",             -- 布尔类型图标（true/false 相关标记、条件判断语法高亮）
--   Class = "蘄",               -- 类类型图标（面向对象编程中「类」的标记、代码结构导航）
--   CodeAction = "蘕",          -- 代码操作图标（LSP 代码修复建议、快速重构功能标记）
--   Color = "",               -- 颜色图标（颜色选择器、CSS 颜色属性提示、主题配置相关）
--   Component = "蘎",           -- 组件图标（前端框架组件，如 Vue/React 组件、UI 组件库标记）
--   Constant = "蘜",            -- 常量图标（代码中常量语法高亮、不可修改变量标记）
--   Constructor = "蘝",         -- 构造函数图标（类的构造方法标记、实例创建相关提示）
--   Definition = "蘞",          -- 定义图标（跳转到定义功能标记、代码定义导航）
--   Diagnostic = "蘟",          -- 诊断图标（LSP 诊断信息总标记、问题排查入口）
--   Dos = "蘠",                 -- Windows 系统图标（系统相关提示、跨系统功能标记）
--   Enum = "蘡",                -- 枚举类型图标（代码中 enum 类型标记、枚举结构导航）
--   EnumMember = "蘢",          -- 枚举成员图标（枚举中的属性/值标记、枚举项选择提示）
--   Error = "蘤",               -- 错误图标（LSP 错误诊断、操作失败提示、严重问题警告）
--   Event = "蘥",               -- 事件图标（代码中事件定义/触发标记、回调函数相关）
--   Field = "蘦",               -- 字段图标（类/对象的属性标记、数据结构字段高亮）
--   File = "",                 -- 文件图标（文件树、文件搜索结果、打开文件标签页标记）
--   Folder = "",               -- 文件夹图标（文件树中的目录标记、目录导航、路径显示）
--   Fragment = "蘨",            -- 代码片段图标（Snippet 插件相关、代码片段插入提示）
--   Function = "蘩",            -- 函数图标（代码中函数定义标记、函数调用导航、语法高亮）
--   Hint = "蘪",                -- 提示图标（LSP 辅助提示信息、可选操作建议）
--   Info = "蘯",                -- 信息图标（普通通知、说明类提示、非关键信息反馈）
--   Interface = "蘬",           -- 接口图标（面向对象编程中「接口」标记、协议定义导航）
--   Key = "蘭",                 -- 键/关键字图标（代码中关键字、字典的键标记、配置项关键词）
--   Keyword = "蘮",             -- 关键字图标（语法高亮中的关键字标记、语言保留字）
--   LspSagaFinder = "גּ",       -- LspSaga 搜索图标（LspSaga 插件的查找/导航功能标记）
--   Mac = "蘰",                 -- MacOS 系统图标（系统相关提示、跨系统功能标记）
--   Method = "蘱",              -- 方法图标（类中的方法标记、对象方法调用导航、语法高亮）
--   Module = "蘲",              -- 模块图标（代码中模块/包的标记、导入模块提示、依赖管理）
--   Namespace = "蘳",           -- 命名空间图标（代码中命名空间标记、作用域隔离提示）
--   Null = "蘴",                -- 空值图标（null/undefined/nil 类型标记、空数据提示）
--   Number = "蘵",              -- 数字类型图标（代码中数字类型标记、数值相关语法高亮）
--   Object = "蘶",              -- 对象图标（代码中对象类型标记、键值对数据结构提示）
--   Operator = "蘷",            -- 运算符图标（语法高亮中的运算符标记、计算/比较操作符）
--   Package = "蘸",             -- 包图标（依赖包、插件包标记、包管理工具相关）
--   Property = "蘸",            -- 属性图标（对象/类的属性标记、配置项属性、数据字段）
--   Reference = "蘹",           -- 引用图标（代码中变量/函数的引用标记、跨文件引用导航）
--   Regex = "蘺",               -- 正则表达式图标（正则相关语法高亮、正则匹配功能标记）
--   Snippet = "蘻",             -- 代码片段图标（代码片段插入提示、自定义代码模板标记）
--   String = "蘼",              -- 字符串类型图标（代码中字符串类型标记、文本数据高亮）
--   Struct = "蘽",              -- 结构体图标（代码中结构体类型标记、复合数据结构导航）
--   Text = "蘾",                -- 文本图标（普通文本内容标记、纯文本文件/字段提示）
--   TypeParameter = "蘿",       -- 类型参数图标（泛型编程中的类型参数标记、模板类型提示）
--   Unit = "蘿",                -- 单位图标（代码中单位相关属性标记、数值单位提示）
--   Value = "籀",               -- 值图标（普通值类型标记、常量/变量的取值提示）
--   Variable = "籁",            -- 变量图标（代码中变量定义标记、可修改数据高亮）
--   Warn = "籂",                -- 警告图标（LSP 警告诊断、潜在问题提示、非致命错误反馈）
--   Watch = "籃",               -- 监听/观察图标（监听事件、数据监听功能、调试观察点标记）
--   Window = "籃",              -- 窗口图标（窗口操作、新窗口打开、多窗口管理相关）
--   Yield = "籃",               -- yield 图标（生成器函数、异步操作暂停标记、迭代器相关）
-- }

-- ==============================================
-- Linux FiraCode 兼容版图标配置（核心优化）
-- 解决痛点：已安装 FiraCode 但 Nerd Font 图标乱码，无需额外安装字体补丁
-- 设计原则：
-- 1. 选用 FiraCode 原生支持的 Unicode 符号（无乱码、渲染清晰）
-- 2. 保持原图标命名和功能对应（替换后所有插件自动适配，无需修改其他配置）
-- 3. 风格统一：以简洁、直观的符号为主，贴合代码编辑场景
-- 使用方法：
-- 1. 取消下方注释（去掉 `-- Ice.symbols = {` 前的 `--`）
-- 2. 确保终端已设置 FiraCode 为默认字体
-- 3. 保存文件并重启 Neovim，图标自动生效
-- ==============================================
Ice.symbols = {
  Affirmative = "✓",          -- 确认/成功标记（FiraCode 原生支持，清晰醒目）
  Array = "[]",               -- 数组类型图标（方括号直观表示数组，无乱码）
  Boolean = "◊",              -- 布尔类型图标（菱形符号，区分于其他类型）
  Class = "⦿",                -- 类类型图标（实心圆代表类的封装特性）
  CodeAction = "⚡",           -- 代码操作图标（闪电符号代表快速修复/重构）
  Color = "🎨",                -- 颜色图标（调色盘符号，直观对应颜色功能）
  Component = "🗂️",            -- 组件图标（文件夹分类符号，代表组件归类）
  Constant = "🔒",             -- 常量图标（锁符号代表不可修改）
  Constructor = "🏗️",          -- 构造函数图标（建筑符号代表创建实例）
  Definition = "📍",           -- 定义图标（定位符号代表跳转到定义）
  Diagnostic = "ℹ️",           -- 诊断图标（信息符号代表诊断提示）
  Dos = "🪟",                  -- Windows 系统图标（窗口符号直观识别）
  Enum = "🔢",                 -- 枚举类型图标（数字序列符号代表枚举）
  EnumMember = "🔘",           -- 枚举成员图标（圆点代表枚举项）
  Error = "❌",                -- 错误图标（叉号代表严重问题，FiraCode 完美渲染）
  Event = "⚡",                -- 事件图标（闪电符号代表事件触发）
  Field = "📌",                -- 字段图标（图钉符号代表对象属性）
  File = "📄",                 -- 文件图标（文件符号直观识别）
  Folder = "📁",               -- 文件夹图标（文件夹符号无乱码）
  Fragment = "🧩",             -- 代码片段图标（拼图符号代表代码片段）
  Function = "🔧",             -- 函数图标（工具符号代表功能实现）
  Hint = "💡",                 -- 提示图标（灯泡符号代表辅助建议）
  Info = "ℹ️",                 -- 信息图标（信息符号代表普通通知）
  Interface = "🔗",            -- 接口图标（链接符号代表接口关联）
  Key = "🔑",                  -- 键/关键字图标（钥匙符号代表关键信息）
  Keyword = "🔤",              -- 关键字图标（字母符号代表语法关键字）
  LspSagaFinder = "🔍",        -- LspSaga 搜索图标（放大镜代表查找功能）
  Mac = "🍎",                  -- MacOS 系统图标（苹果符号直观识别）
  Method = "⚙️",               -- 方法图标（齿轮符号代表类的方法）
  Module = "📦",               -- 模块图标（包裹符号代表模块/包）
  Namespace = "📂",            -- 命名空间图标（文件夹符号代表作用域）
  Null = "∅",                  -- 空值图标（空集符号代表 null/undefined）
  Number = "🔢",               -- 数字类型图标（数字符号代表数值）
  Object = "🗃️",               -- 对象图标（文件柜符号代表键值对结构）
  Operator = "➕",              -- 运算符图标（加号代表运算操作，通用直观）
  Package = "📦",              -- 包图标（包裹符号代表依赖包）
  Property = "📌",             -- 属性图标（同 Field，保持一致性）
  Reference = "↩️",             -- 引用图标（返回符号代表引用关联）
  Regex = "🔍",                -- 正则表达式图标（放大镜代表匹配功能）
  Snippet = "🧩",              -- 代码片段图标（同 Fragment，保持一致性）
  String = "📝",               -- 字符串类型图标（笔记符号代表文本）
  Struct = "🏗️",               -- 结构体图标（建筑符号代表复合结构）
  Text = "📄",                 -- 文本图标（文件符号代表纯文本）
  TypeParameter = "🔠",        -- 类型参数图标（字母符号代表泛型参数）
  Unit = "📏",                 -- 单位图标（尺子符号代表数值单位）
  Value = "💎",                -- 值图标（宝石符号代表数据值）
  Variable = "🔄",             -- 变量图标（循环符号代表可修改）
  Warn = "⚠️",                 -- 警告图标（警告符号代表潜在问题，醒目无乱码）
  Watch = "👁️",               -- 监听/观察图标（眼睛符号代表监听功能）
  Window = "🪟",               -- 窗口图标（窗口符号代表窗口操作）
  Yield = "➡️",                -- yield 图标（箭头符号代表迭代/暂停）
}